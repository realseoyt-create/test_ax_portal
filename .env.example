DATABASE_URL="file:./dev.db"
UPLOAD_DIR="public/uploads"
PORT=3000
# Shared code required to register a new AX 스타터 키트 item, until SSO-based
# admin roles replace this. Set to a real secret outside of source control.
ADMIN_CODE="change-me"
